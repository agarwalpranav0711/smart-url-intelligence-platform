# Public API Base URL configuration for local Vite development
# NOTE: VITE_* variables are embedded in the bundle at build time.
# NEVER place secret keys, passwords, or server credentials in VITE_* variables.
VITE_API_BASE_URL=http://localhost:3000
VITE_PUBLIC_BASE_URL=http://localhost:3000
